import type {JournalEntry} from '../types/trading';export function DecisionJournal({journal}:{journal:JournalEntry[]}){return <div className="page"><div className="page-head"><div><div className="eyebrow">DECISION JOURNAL</div><h2>Every AI decision, preserved</h2></div></div><section className="panel"><div className="panel-title">AUDIT TRAIL</div>{journal.length?journal.map(j=><article className="journal" key={j.id}><div><b>{j.symbol}</b><span>{new Date(j.created_at).toLocaleString()}</span></div><p>{j.thesis}</p><div className="journal-meta"><span>REGIME {j.regime}</span><span>SCORE {j.score}</span><span>{j.risk_approved?'RISK PASS':'RISK VETO'}</span><span>{j.executed?'EXECUTED':'NOT EXECUTED'}</span></div></article>):<div className="empty">No decisions yet.</div>}</section></div>}
