import {RegimePanel} from '../components/RegimePanel';import {RiskPanel} from '../components/RiskPanel';import {DecisionPanel} from '../components/DecisionPanel';import type {JournalEntry,AccountSnapshot} from '../types/trading';
export function CommandCenter({account,journal}:{account:AccountSnapshot|null;journal:JournalEntry[]}){const x=journal[0];const a=account||{portfolio_value:0,buying_power:0,daily_pnl:0,open_risk:0,underlying_exposure:0,position_count:0};return <div className="page"><div className="page-head"><div><div className="eyebrow">COMMAND CENTER</div><h2>Market → Thesis → Risk → Execution</h2></div><span className="live-dot">LIVE PIPELINE</span></div><div className="grid-3"><RegimePanel regime={x?.regime||'UNKNOWN'}/><RiskPanel account={a}/><DecisionPanel approved={x?.risk_approved||false} score={x?.score||0} reason={x?.veto_reason||null}/></div><section className="panel"><div className="panel-title">LATEST DECISIONS</div>{journal.length?<div className="table-wrap"><table><thead><tr><th>TIME</th><th>SYMBOL</th><th>REGIME</th><th>SCORE</th><th>RISK</th><th>EXECUTION</th></tr></thead><tbody>{journal.slice(0,8).map(j=><tr key={j.id}><td>{new Date(j.created_at).toLocaleTimeString()}</td><td><b>{j.symbol}</b></td><td>{j.regime}</td><td>{j.score}</td><td>{j.risk_approved?'PASS':'VETO'}</td><td>{j.executed?'EXECUTED':'NO ORDER'}</td></tr>)}</tbody></table></div>:<div className="empty">No decisions recorded yet.</div>}</section></div>}
