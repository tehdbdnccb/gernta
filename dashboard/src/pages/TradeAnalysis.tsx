import {useState} from 'react';import {tradingApi} from '../api/trading';export function TradeAnalysis(){const [id,setId]=useState('');const [data,setData]=useState<unknown>();const [error,setError]=useState('');async function load(){try{setError('');setData(await tradingApi.analysis(id))}catch(e){setError(e instanceof Error?e.message:'Run not found')}}return <div className="page"><div className="page-head"><div><div className="eyebrow">TRADE ANALYSIS</div><h2>Inspect the complete decision chain</h2></div></div><section className="panel"><div className="search"><input value={id} onChange={e=>setId(e.target.value)} placeholder="Run ID"/><button onClick={load}>Load</button></div>{error&&<div className="error">{error}</div>}{data&&<pre className="json">{JSON.stringify(data,null,2)}</pre>}</section></div>}
