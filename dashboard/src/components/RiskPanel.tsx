export function RiskPanel({account}:{account:{daily_pnl:number;open_risk:number;portfolio_value:number}}){const rows=[['Daily P&L',account.daily_pnl,'2% max daily loss'],['Open Risk',account.open_risk,'5% max'],['Trade Risk',account.portfolio_value*.01,'1% max trade']];return <section className="panel"><div className="panel-title">RISK KERNEL</div>{rows.map(([a,b,c])=><div className="metric" key={a as string}><div><span>{a}</span><b>${Number(b).toLocaleString(undefined,{maximumFractionDigits:2})}</b></div><small>{c}</small></div>)}</section>}
